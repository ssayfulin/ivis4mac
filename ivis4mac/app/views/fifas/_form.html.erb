<%= form_for(@fifa) do |f| %>
  <% if @fifa.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@fifa.errors.count, "error") %> prohibited this fifa from being saved:</h2>

      <ul>
      <% @fifa.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :first_name %><br />
    <%= f.text_field :first_name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
