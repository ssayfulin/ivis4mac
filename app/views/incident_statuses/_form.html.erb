<%= form_for(@incident_status) do |f| %>
  <% if @incident_status.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@incident_status.errors.count, "error") %> prohibited this incident_status from being saved:</h2>

      <ul>
      <% @incident_status.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
