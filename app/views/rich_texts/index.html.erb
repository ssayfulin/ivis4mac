<h1>Listing rich_texts</h1>

<table>
  <tr>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @rich_texts.each do |rich_text| %>
  <tr>
    <td><%= link_to 'Show', rich_text %></td>
    <td><%= link_to 'Edit', edit_rich_text_path(rich_text) %></td>
    <td><%= link_to 'Destroy', rich_text, confirm: 'Are you sure?', method: :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Rich text', new_rich_text_path %>
