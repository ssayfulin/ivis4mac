<%= form_for(@map_link) do |f| %>
  <% if @map_link.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@map_link.errors.count, "error") %> prohibited this map_link from being saved:</h2>

      <ul>
      <% @map_link.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
