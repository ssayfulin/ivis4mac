<h1>Listing sign_types</h1>

<table>
  <tr>
    <th>Sign type</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @sign_types.each do |sign_type| %>
  <tr>
    <td><%= sign_type.sign_type %></td>
    <td><%= link_to 'Show', sign_type %></td>
    <td><%= link_to 'Edit', edit_sign_type_path(sign_type) %></td>
    <td><%= link_to 'Destroy', sign_type, confirm: 'Are you sure?', method: :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Sign type', new_sign_type_path %>
