<%= form_for(@sign_value) do |f| %>
  <% if @sign_value.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@sign_value.errors.count, "error") %> prohibited this sign_value from being saved:</h2>

      <ul>
      <% @sign_value.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :sign_value %><br />
    <%= f.text_field :sign_value %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
